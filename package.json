{
  "name": "champion-steno-lightboard",
  "version": "0.1.0",
  "engines": {
    "node": "10.x",
    "npm": "6.4.1"
  },
  "private": true,
  "dependencies": {
    "cors": "^2.8.4",
    "cowsay": "^1.3.1",
    "express": "^4.16.3",
    "express-fileupload": "^1.0.0-alpha.1",
    "http": "0.0.0",
    "path": "^0.12.7",
    "ws": "^6.0.0"
  },
  "scripts": {
    "start": "node app.js",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  }
}
